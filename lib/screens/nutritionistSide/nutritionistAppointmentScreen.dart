import 'package:flutter/material.dart';

class NutritionistAppointmentScreen extends StatelessWidget {
  static const routeName= 'nutritionist-appointment-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Nutritionist Appointment Screen'),
      ),
    );
  }
}